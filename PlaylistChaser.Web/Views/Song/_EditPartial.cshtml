@using PlaylistChaser.Web.Util;
@model PlaylistChaser.Web.Models.Song

<div class="row">
    <div class="col-12">
        <form asp-action="_EditPartial">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />

            <div class="form-group">
                @Html.BsLabelFor(model => model.SongName)
                @Html.BsTextBoxFor(m => m.SongName)
            </div>
            <div class="form-group">
                @Html.BsLabelFor(model => model.ArtistName)
                @Html.BsTextBoxFor(m => m.ArtistName)
            </div>
            <div class="form-group">
                @Controls.SubmitButton()
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
